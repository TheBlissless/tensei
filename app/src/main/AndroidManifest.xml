<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Manga extension discovery: allows querying broadcast receivers for the
         EXTENSION_BEACON intent so we can find installed manga source extensions. -->
    <queries>
        <intent>
            <action android:name="com.blissless.mangaclient.EXTENSION_BEACON" />
        </intent>
    </queries>

    <application
        android:name=".TenseiApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.App.Launch"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|layoutDirection|keyboardHidden|uiMode"
            android:supportsPictureInPicture="true">

            <intent-filter android:label="AniList Auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="animescraper"
                    android:host="success" />
            </intent-filter>

        </activity>

        <activity-alias
            android:name=".MainActivityDefault"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_default"
            android:roundIcon="@mipmap/ic_launcher_default"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Launch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityWob"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_wob"
            android:roundIcon="@mipmap/ic_launcher_wob"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Launch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityBow"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_bow"
            android:roundIcon="@mipmap/ic_launcher_bow"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Launch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity-alias>

        <receiver
            android:name=".widget.AiringScheduleWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/airing_schedule_widget_info" />
        </receiver>

        <!-- ── Aniyomi-style extension install receiver. Listens for
             system package install/replace/remove broadcasts and
             re-triggers AnimeExtensionManager.initAnimeExtensions()
             so newly installed extensions show up automatically.
             Declared statically so it works even when the app process
             isn't running yet (cold start). -->
        <receiver
            android:name=".extensions.AnimeExtensionInstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Foreground service that receives the result callback for
             PackageInstaller-based extension installs (the
             PACKAGEINSTALLER installer mode in AnimeExtensionInstaller).
             Not required for LEGACY or PRIVATE installers but harmless
             to declare. -->
        <service
            android:name=".extensions.AnimeExtensionInstallService"
            android:exported="false" />

        <activity
            android:name=".stream.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MyApplication" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
